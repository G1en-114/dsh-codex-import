# dsh-codex-import bundle patch: registers the /codex-import command row.
#
# Because this package is a bundle (package.json "dsh.bundle.patch"), running
# `dsh plugin --profile web add <this package>` appends it to the profile's
# bundle list automatically — no manual row editing needed.
- insert:
    - id: codex-import
      name: 'dsh-codex-import'
